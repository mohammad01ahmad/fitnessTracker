node_modules/
.env
.env.local
.next/
# glob, not an exact name: `auth_session.bak` slipped past the bare
# `auth_session/` rule and got committed on 2026-07-31.
backend/auth_session*/
.DS_Store

.claude/
